<!DOCTYPE html>
<html>
  <head>
    <title>Boatdocker</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="shadow pb-3">
      <div class="container mx-auto px-5 mt-5">
        <ul class="flex">
          <li class="mr-6">
            <a class="text-blue-500 hover:text-blue-800 <%= "underline font-semibold" if current_page?(boats_path) %>" href="<%= boats_path %>">Boats</a>
          </li>
          <li class="mr-6">
            <a class="text-blue-500 hover:text-blue-800 <%= "underline font-semibold" if current_page?(members_path) %>" href="<%= members_path %>">Members</a>
          </li>
          <li class="mr-6">
            <a class="text-blue-500 hover:text-blue-800 <%= "underline font-semibold" if current_page?(slots_path) %>" href="<%= slots_path %>">Slots</a>
          </li>
        </ul>
      </div>
    </div>
    <main class="container mx-auto mt-28 px-5 flex-1">
    
      <%= yield %>
    </main>
  </body>
</html>
